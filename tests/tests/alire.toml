name = "tests_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
tests = "*"

[[pins]]
tests = { path = ".." }

[build-profiles]
tests       = "validation"
tests_tests = "validation"
